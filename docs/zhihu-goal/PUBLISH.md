# 知乎专栏粘贴说明

知乎不渲染 mermaid，也不显示 SVG。本文配图已是 PNG，按顺序在编辑器里上传后，再贴正文。

## 标题

给 Agent 加长期目标：进展可以记住，开机不准自己跑

## 导语

给 Agent 加长期目标，不要改 loop。状态可回放，权限必须重授——Kilo 选了一行表当真相，但开机仍要人点继续。能抄走的是这套切割，不是文件名。

正文结构：标题下 TL;DR → 错路/脉络/树 → 收获和结论 → PS / PPS / PPPS。

## 配图上传顺序（12 张）

1. `images/09-wrong-loop.png` — 错路 / 正路
2. `images/00-thread.png` — 逻辑脉络
3. `images/01-tree.png` — 整棵树
4. `images/04-seam.png` — 一个服务、三个消费者
5. `images/08-two-stores.png` — 两种记账法，Kilo 走杂交
6. `images/06-cas.png` — 同时改一份账
7. `images/03-cut.png` — 走到哪 vs 准不准续
8. `images/05-authority.png` — 谁能改
9. `images/07-round.png` — 一次一续
10. `images/10-evidence.png` — 证据先行
11. `images/03-branches.png` — 四根分支各归一处
12. `images/11-takeaway.png` — 收获七条

`images/02-is-not.png` 是上一稿的总对照表，正文不再引用；粘贴时不必上传。

正文里的 `![…](images/….png)` 粘贴后不会自动带上本地文件；在对应位置插入刚上传的图即可。

## 标签建议

Agent、LLM、系统设计、开源
