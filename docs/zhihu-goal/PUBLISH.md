# 知乎专栏粘贴说明

知乎不渲染 mermaid，也不显示 SVG。本文配图已是 PNG，按顺序在编辑器里上传后，再粘贴正文。

## 标题

给 AI 编码代理加一个「长跑目标」：别改主循环，改的是一组正交约束

## 导语（30–60 字，可作开头摘要）

给 Agent 加长期目标，不要改 loop、不要写进提示词。Kilo Code 落地长跑目标的做法是：状态可回放，权限必须重授。

## 配图上传顺序

1. `images/01-goal-agent.png` — 一个目标，四根正交分支
2. `images/02-store.png` — 一行一个目标：kilo_goal 持久化
3. `images/03-driver.png` — 空转续跑：一次一次续，不忙循环

正文里的 `![…](images/…png)` 粘贴后不会自动带上本地文件；在对应位置插入刚上传的图即可。

## 标签建议

Agent、LLM、系统设计、开源
